appId: com.aikobox.app
productName: AikoBox
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!aur/*'
  - '!images/*'
  - '!scripts/*'
  - '!extra/*'
  - '!tailwind.config.js'
  - '!postcss.config.js'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
extraResources:
  - from: './extra/'
    to: ''
protocols:
  name: 'AikoBox URI Scheme'
  schemes:
    - 'clash'
    - 'mihomo'
    - 'aikobox'
win:
  target:
    - nsis
    - 7z
  artifactName: aikobox-windows-${version}-${arch}-portable.${ext}
nsis:
  artifactName: aikobox-windows-${version}-${arch}-setup.${ext}
  uninstallDisplayName: ${productName}
  allowToChangeInstallationDirectory: true
  oneClick: false
  perMachine: true
  createDesktopShortcut: always
mac:
  target:
    - pkg
  entitlementsInherit: build/entitlements.mac.plist
  hardenedRuntime: true
  gatekeeperAssess: false
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
  artifactName: aikobox-macos-${version}-${arch}.${ext}
pkg:
  allowAnywhere: false
  allowCurrentUserHome: false
  isRelocatable: false
  background:
    alignment: bottomleft
    file: build/background.png
linux:
  executableName: aikobox
  icon: build/icon.png
  desktop:
    entry:
      Name: AikoBox
      GenericName: Proxy Client
      Comment: AikoBox Proxy Client
      MimeType: 'x-scheme-handler/clash;x-scheme-handler/mihomo;x-scheme-handler/aikobox'
      Keywords: proxy;clash;sing-box;aikobox;vpn;
      StartupWMClass: aikobox
      Icon: aikobox
  target:
    - deb
    - rpm
  maintainer: aikobox
  category: Utility
  artifactName: aikobox-linux-${version}-${arch}.${ext}
deb:
  afterInstall: 'build/linux/postinst'
  afterRemove: 'build/linux/postuninst'
rpm:
  afterInstall: 'build/linux/postinst'
  afterRemove: 'build/linux/postuninst'
  fpm:
    - '--rpm-rpmbuild-define'
    - '_build_id_links none'
npmRebuild: true
publish: []
