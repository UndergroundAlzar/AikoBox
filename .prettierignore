out
dist
.claude
pnpm-lock.yaml
LICENSE.md
tsconfig.json
tsconfig.*.json
# Generated by Gradle / the Dart toolchain in the Android client. Same reason
# `out` and `dist` are here: nobody edits these, and they are not committed.
mobile/build
mobile/android/build
mobile/**/.dart_tool
mobile/**/.flutter-plugins-dependencies

# Golden output of the TypeScript converter, emitted by tool/dump_ts_cases.mts and compared
# byte-for-byte against the Dart port. The bytes ARE the contract, so a formatter must not
# rewrite them — it would silently absorb a real divergence between the two implementations.
mobile/packages/aikobox_convert/test/parity

# Minimal Android release client
apps/android/.dart_tool/
apps/android/build/
apps/android/android/.gradle/
apps/android/android/build/
apps/android/android/local.properties
apps/android/android/key.properties
apps/android/android/app/libs/*.aar
