<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Reading the SSID for the "pause on this network" feature ported from the desktop app. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!--
      Split tunnelling is a per-app allow/deny picker, so the picker has to be able to
      enumerate what is installed. A <queries> block cannot do that — it only makes
      already-known packages visible. This is a Play-policy-restricted permission and the
      listing must declare the VPN use case; InstalledAppsReader degrades to whatever the
      platform will hand it if the permission is ever refused.
    -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:label="AikoBox"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Subscription import deep links, matching the desktop protocol registration
                 in electron-builder.yml. Every one of these is gated behind a native
                 confirmation dialog that defaults to Cancel. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="clash" android:host="install-config" />
                <data android:scheme="mihomo" android:host="install-config" />
                <data android:scheme="aikobox" android:host="install-config" />
            </intent-filter>
        </activity>

        <!--
          The core runs in its own :remote process so a crash inside the Go runtime cannot
          take the Flutter UI down with it, and so the UI can be killed and restored while
          the tunnel stays up.
        -->
        <service
            android:name=".service.AikoVpnService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:process=":remote">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <meta-data
                android:name="android.net.VpnService.SUPPORTS_ALWAYS_ON"
                android:value="true" />
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="AikoBox routes this device's network traffic through a user-configured proxy using Android VpnService. The tunnel must stay active while the screen is off and while other apps are in the foreground, which no other foreground service type covers." />
        </service>

        <!--
          Quick-settings tile. Stays in the default process on purpose: it learns the
          tunnel's state from the app-internal CORE_STATE broadcast, not from memory shared
          with :remote, so putting it in :remote would only cost a second process wake-up.
        -->
        <service
            android:name=".service.QuickTileService"
            android:exported="true"
            android:icon="@drawable/ic_stat_tunnel"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!--
          Restores the tunnel after a reboot when it was up beforehand. :remote is required,
          not cosmetic — it reads the `aikobox.remote` SharedPreferences file the service
          writes, and Android does not keep that coherent across processes.
        -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true"
            android:process=":remote">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
